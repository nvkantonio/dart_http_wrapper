name: http_wrapper
description: A starting point for Dart libraries or applications.
version: 0.0.1

environment:
  sdk: ^3.8.1

dependencies:
  http: ^1.5.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
